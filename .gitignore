# Claude Code local state / agent worktrees (but track the committed hook + settings)
.claude/*
!.claude/hooks/
!.claude/settings.json

# Build output (ask before building artifacts)
build/
dist/

# Ada/SPARK object & proof dirs
obj/
obj_prove/
obj_tests/

# Python caches should not be ever
